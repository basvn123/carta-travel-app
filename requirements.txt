# src/ingestion raw data collection framework (python -m src.ingestion.run_all)
requests>=2.32
httpx>=0.27
beautifulsoup4>=4.12
lxml>=5.2
pandas>=2.2
kaggle>=1.6
python-dotenv>=1.0

# src/estimation fare model: quantile GBDT + PSI/KS drift (train/drift/estimate)
scikit-learn>=1.4

# pipeline/trails content lab (tools/trailslab local PostGIS staging DB)
psycopg[binary]>=3.2
# pipeline/trails/ingest_osm_routes.py: Geofabrik .osm.pbf route extraction
osmium>=4.0
# pipeline/trails/crosscheck_portals.py: portal geometry staging (WKB for COPY)
shapely>=2.0
# pipeline/trails/elevation.py: Copernicus GLO-30 COG sampling
rasterio>=1.4
# pipeline/trails/describe.py: Claude API as a rewriter over a facts block.
# Only needed for --provider claude; --provider gemini is plain REST.
anthropic>=0.69
# tools/trailslab/review: local-only review API (binds 127.0.0.1)
fastapi>=0.115
uvicorn>=0.30
